
//const length = []
//const upperCase = [length >= 1]
//const lowerCase = [length >= 1]
//const number = [length >= 1]
//const character = [length >= 1]

const validatePassword = (number, upperCase, lowerCase, length, character) => {
   let valid = []
//infput: 5 vairables
    for (i = 0; i < arr.length; i++) {

   if (valid = (arr.length <= 8)){
       return false
   }

    if (valid = (!lowerCase && !upperCase) || (!lowerCase && upperCase) || (lowerCase && !upperCase)){
        return false
    }

    if(valid = (number <= 1)) {
        return fales
    }

    if (valid = (character <= 1)) {
        return false
    }

    else {
        return true
    }

    module.exports = validatePassword
